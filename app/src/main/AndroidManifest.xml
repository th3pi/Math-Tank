<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nyc.tanjim.mathtank">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="MainMenu"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Advanced" />
        <activity android:name="QuickMath" />
        <activity android:name="QuickMathLoadingScreen" />
        <activity android:name="TimeTrials" />
        <activity android:name="TimeTrialsLoadingScreen" />
        <activity android:name="AdvancedLoadingScreen" />
        <activity
            android:name="SettingsActivity"
            android:label="Settings"
            android:parentActivityName="MainMenu">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="MainMenu" />
        </activity>
        <activity android:name="AppInfo"></activity>
        <service android:name="nyc.MusicService"/>
    </application>

</manifest>